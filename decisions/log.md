# Decision Log

| Date | Decision | Rationale | Status |
|------|----------|-----------|--------|
| 2026-04-26 | ใช้ Claude Course dashboard (splendorous-mandazi-121580.netlify.app) เป็น recruitment vehicle สำหรับ Art และ Soda | ให้เพื่อนเรียน AI ร่วมกันก่อน → build shared context → pitch Senior Project ง่ายขึ้น | Active |
| 2026-04-26 | Bond ทำงานแบบ Creative Director model — AI implement+debug ทั้งหมด Bond approve/reject ด้วย feel | Bond ไม่อ่าน code และไม่ debug เอง — AI ต้องจัดการให้ครบจนได้ผลลัพธ์ที่ work | Confirmed |
| 2026-04-26 | Business analysis ต้องใช้ multi-agent research-first approach — research → verify กับ Bond → synthesize | AI วิเคราะห์แบบ shallow มาตลอด; รูปแบบนี้แก้ปัญหานั้นได้ตรงจุด | Confirmed |
| 2026-04-26 | ก่อน implement ทุกครั้งต้องถาม discovery questions ก่อน — ไม่ guess แล้ว charge ahead | Bond มี 2 mode: มี mental wireframe หรือมีแค่ feeling — ทั้งสองต้องการ alignment ก่อน | Confirmed |
| 2026-04-26 | RLS ของ Claude Course dashboard เปิด open access (ไม่มี auth) — ตัดสินใจไม่แก้ | กลุ่มเล็ก 3 คน (Bond, Art, Soda), ความเสี่ยงต่ำ, login system ยุ่งยากเกินกว่าคุ้ม | Active |
| 2026-04-28 | สร้าง `scratch/` folder สำหรับ ephemeral inputs (transcripts, images, one-off docs) | Bond ต้องการ workspace ชั่วคราวที่ไม่ pollute ระบบ Second Brain; Stop hook auto-detect แล้วถาม Bond ก่อนลบ | Implemented |
| 2026-04-28 | NotebookLM integration Phase 1 ทำทันที, Pinecone รอ Phase 2 | Bond มี 80+ notebooks ที่ใช้งานได้เลย; เพิ่ม Pinecone ก่อน Phase 1 stable = complexity ไม่คุ้ม | Decided |
| 2026-04-28 | WrapUp mode: Augment — คง .md system + เพิ่ม NotebookLM เป็น layer 2 | .md = working memory (fast), NotebookLM AI Brain = long-term semantic archive (compound value ตามเวลา) | Decided |
| 2026-04-28 | Notebook routing ผ่าน registry file (`references/notebooklm-registry.md`) | Bond มี 80+ notebooks หลาย domain; hardcoded routing ไม่ scale | Implemented |
| 2026-04-28 | Claude query NotebookLM on-demand เท่านั้น ไม่ใช่ทุก message | Auto-query ทุก message = latency + noise ไม่จำเป็น; on-demand ผ่าน skill trigger เพียงพอ | Decided |
| 2026-04-28 | WrapUp auto-trigger จาก phrases ธรรมชาติ ("โอเค จบแล้ว" ฯลฯ) | Bond ไม่อยากจำ /wrapup; automation ลด friction และ zero amnesia ทำงานได้จริง | Implemented |
| 2026-04-29 | Bundle Skill architecture สำหรับ complex skills ทุกตัว | Independent phases + quality gate = output ที่ดีกว่า single-pass อย่างมีนัยสำคัญ | Decided |
| 2026-04-29 | Merge /biz-model เข้า /consult | Bond ไม่ต้องการ skills ที่ capabilities ทับกัน; consult handle biz model ผ่าน Hormozi routing ได้ | Decided |
| 2026-04-29 | Skill files เขียนเป็น English ทั้งหมด | Bond explicit: ให้ Claude อ่านและ follow ได้แม่นยำกว่า; technical clarity สำคัญกว่า language preference | Decided |
| 2026-04-29 | Quality threshold 95% สำหรับ Bundle Skills ทั้งหมด | สูงพอที่จะรับประกัน quality; ต่ำพอที่จะผ่านได้ใน ≤2 loops โดยไม่ต้อง loop ตลอดกาล | Decided |
| 2026-04-29 | Skill auto-invoke = mandatory เมื่อ context match | Bond build skills เพื่อไม่ต้องจำ command; ถ้าต้องบอก Claude ให้ invoke = defeats the purpose | Confirmed |
| 2026-04-29 | Query notebooks โดยตรงก่อน build routing table | Bond's descriptions อาจ inaccurate; ground truth จาก NB itself = routing ที่แม่นกว่า | Confirmed |
| 2026-04-29 | Second Brain — GO (มีเงื่อนไข): commit ทำ Senior Project + เดิน path ไป startup | Data ยืนยัน: niche ราชการไทย big tech ไม่ลงมาเล่น, NIA grant accessible, window 18–24 เดือน; เงื่อนไข: ต้องยืนยัน budget authority ของห้องธุรการก่อน commit เต็มตัว | Decided |
| 2026-04-29 | ปลี่ยน external product name: "Second Brain" → "Thai Government Document AI" (ชั่วคราว) | "Second Brain" = Tiago Forte's brand ทำให้ investor เสีย 10 นาทีแรกกับ differentiation; ชื่อ beachhead ที่ชัดกว่าตัด noise ออก | Decided |
| 2026-04-29 | Series A timeline = 2.5–3 ปีจากวันนี้ (ไม่ใช่ 1–2 ปี) | Thai VC path: NIA → Thai angel → Beacon/500TT → regional VC → Peak XV/Openspace; ไม่มี shortcut ข้ามขั้น | Decided |
| 2026-04-29 | Language rule: mirror Bond's input language (ถ้าพิมพ์ไทย ตอบไทย; ถ้าพิมพ์ English ตอบ English) | กฎเดิมซับซ้อนเกินไป; กฎนี้ง่ายและ intuitive กว่า; exception เฉพาะเอกสารสากล | Confirmed |
| 2026-04-29 | WrapUp Step 6: push rich NB knowledge document แทน session summary bullet สั้น | Session summary = Bond skim เร็ว; NB document = semantic retrieval สำหรับ future sessions; สองอย่างต้องการ detail level ต่างกัน | Decided |
| 2026-04-29 | Second Brain vision = AI for work ของทุกคน (ไม่ใช่แค่ Thai gov docs) | Bond clarify: "Thai Government Document AI" เป็น beachhead pitch เท่านั้น; vision จริงคือ AI ส่วนตัวสำหรับทุกคนในการทำงาน คล้าย Claude Code สำหรับ non-tech users | Clarified |
| 2026-04-29 | Merge `skill-builder` + `skill-creator` เป็น `skill-builder` ตัวเดียว | skill-builder มี design/architecture แต่ไม่มี eval loop; skill-creator มี eval/iteration แต่ไม่มี discovery interview หรือ Bundle Skill; merged = end-to-end workflow ที่ครบกว่าทั้งคู่ | Implemented |
| 2026-04-30 | Skill naming convention: ไม่ใช้ "bond-" prefix → ใช้ professional descriptive names (ceo-advisor, ops-advisor ฯลฯ) | "bond-" prefix ดู hacky ไม่ professional; descriptive names อ่านแล้วรู้ function ทันที | Decided |
| 2026-04-30 | All skills in `.claude/skills/` เท่ากันหมด — ไม่มี `_future/` subfolder | Skills ที่ "ยังไม่ถึงเวลา" ยังมีคุณค่า (org-advisor, investor-advisor); การแบ่งโฟลเดอร์เพิ่ม friction ไม่จำเป็น | Decided |
| 2026-04-30 | Skip 5 infrastructure fakes: context-engine, chief-of-staff, agent-protocol, decision-logger, intl-expansion | ไม่ใช่ skill จริง — เป็น routing/protocol layer ที่ Bond's system (CLAUDE.md + consult/research-brief) ทำแทนได้อยู่แล้ว | Decided |
| 2026-04-30 | Strategy category: 24 source skills → 9 active skills | Merge โดย function group (OKR/exec, competitive, investor, stress-test) ป้องกัน Claude งงว่าจะ invoke อันไหนเมื่อ context ตรงกันหลายอัน | Implemented |
| 2026-04-30 | scratch/skill-lab คง read-only ไว้ — ยังต้องใช้สำหรับ Marketing + Engineering + Product categories | Source material ยังไม่ครบ process; ลบก่อน = ต้อง clone ใหม่ | Active |
| 2026-05-01 | Bundle Skills ทุกตัวต้องมี Quality Gate 95/100 ก่อน deliver | Marketing output ออกถึง users/investors จริง; half-baked output = brand damage; เหมือน consult ที่ใช้ 95% อยู่แล้ว | Decided |
| 2026-05-01 | Single-pass skills ใช้ Quality Check 90/100 (ไม่มี loop-back) | Single-pass ไม่ซับซ้อนพอที่ต้องการ full loop; 90 threshold ป้องกัน output แย่โดยไม่เพิ่ม overhead | Decided |
| 2026-05-01 | "ยังไม่ถึงเวลา" skills → BUILD ทั้งหมด (paid-growth-advisor, growth-advisor, popup/paywall → cro-advisor) | Source scratch/skill-lab จะถูกลบหลัง process ครบ; ลงทุนตอนนี้ = ไม่ต้อง rebuild จาก scratch ทีหลัง | Decided |
| 2026-05-01 | Overlap skills → absorb content เข้า existing skills แทนการ skip | marketing-strategy-pmm (ICP+Dunford) → gtm-advisor; competitor-alternatives (vs-pages) → competitive-intel; referral-program → gtm-advisor | Implemented |
| 2026-05-01 | Marketing category: 44 source skills → 8 new + 2 improved = clean non-overlapping architecture | ป้องกัน Claude invoke ผิด skill เมื่อ context ตรงกันหลายอัน; each skill has unique trigger domain | Implemented |
| 2026-05-01 | Bundle Skill Gate เปลี่ยนจาก "recommend" เป็น MUST (hard mandate) ใน skill-builder | Session 5 พบว่า bundle skills ถูก build เป็น flat file เพราะ gate ใช้ภาษา "recommend" — แก้เป็น mandatory rule ป้องกัน recurrence | Implemented |
| 2026-05-01 | Architecture rule สำหรับ skills: Bundle = sequential phases w/ distinct personas → orchestrator + ref files; Flat = independent modes → all-in-SKILL.md + supplementary refs | ชัดเจนว่า pattern ไหนใช้เมื่อไหร่ — ไม่ต้องตีความใหม่ทุก session | Decided |
| 2026-05-01 | Engineering category: 84 source skills → 11 active skills (ทั้งหมดใน 2 sessions) | architect + debug + test + devops + security + api + animate + release ครบ; autoresearch defer ไป dedicated session เพราะ architecture ซับซ้อน | Implemented |
| 2026-05-01 | autoresearch defer ไป dedicated session (ไม่ build inline) | Autonomous self-improvement loop ต้องการ file-based state + context:fork + compressed eval — ถ้า design ผิดจะแย่กว่าไม่มี skill; handoff plan เขียนไว้ใน plan file แล้ว | Implemented |
| 2026-05-01 | `autoresearch` skill built: single SKILL.md, JSON state, ScheduleWakeup loop, Agent subagent per iteration | Multi-file plugin (Python scripts + 5 sub-skills) collapsed ลง 1 ไฟล์ 366 บรรทัด; context isolation ผ่าน Agent subagent แทน context:fork ที่ skill level; token economy บังคับที่ subagent boundary | Implemented |
| 2026-05-01 | /wrapup ต้องรันควบคู่กับทุก file update หรือ save instruction เสมอ | Bond พบว่า file edits อย่างเดียวไม่พอ — memory + decisions/log + NotebookLM push ขาด; /wrapup ปิดช่องว่างนี้; บันทึกเป็น Rule 2 ใน feedback_session_automation.md | Implemented |
| 2026-05-01 | Skill system total: 31 active skills — Strategy (9) + Marketing (10) + Engineering (11) + Core (consult + skill-builder + wrapup + notebooklm) | Engineering category เสร็จสมบูรณ์; next = Product category (16 source skills) | Superseded |
| 2026-05-01 | Product category: 16 source skills → 5 new + 5 absorbed (competitive-teardown/research-summarizer/landing-page-gen/saas-scaffolder/spec-to-repo) | ทำ parallel กับ autoresearch ใน 2 sessions พร้อมกัน — ประหยัดเวลา | Implemented |
| 2026-05-01 | Skill system total: 37 active skills — Strategy(9) + Marketing(10) + Engineering(12 incl. autoresearch) + Product(5) + Core(4) | เหลือ Finance/BG/PM เท่านั้น; RA/QM skip | Status |
| 2026-05-01 | Product category BUILD COMPLETE: product-manager (5 modes), product-strategist (4 modes), product-analytics (5 modes), ux-designer (7 modes), apple-hig (2 modes) + 3 absorptions into existing skills | Flat reference guide pattern ทุกตัว; 27 files total; trigger phrases non-overlapping | Implemented |
| 2026-05-01 | Finance category BUILD COMPLETE: financial-advisor (5 modes: Ratio/Valuation/Forecast/SaaS Metrics/Investment) จาก 3 source skills | ทำให้ครบ finance domain ใน 1 skill; investment decisions absorb เข้า financial-advisor แทนแยก — ไม่ซ้อน investor-advisor | Implemented |
| 2026-05-01 | Business Growth category BUILD COMPLETE: sales-advisor (3 modes: Proposals/Customer Success/RevOps) + sales-engineering ref absorbed เข้า engineer | 4 source skills → 1 skill; sales-engineer เป็น pre-sales workflow ใน engineer ได้เลย | Implemented |
| 2026-05-01 | PM Partial BUILD COMPLETE: meeting-analyzer (lightweight standalone) + agile-capacity.md เข้า product-manager (WSJF/Three Horizons/velocity) + internal-comms.md เข้า org-advisor | แผนเดิม: 1 new + 2 absorptions; ทำครบตามแผน; atlassian skills ทั้งหมด skip ตามที่ตัดสินใจ | Implemented |
| 2026-05-01 | Skill system COMPLETE: 42 active skills ทุก category เสร็จ; scratch/skill-lab ถูกลบแล้ว | ระบบ skill ใช้งานได้จริงแล้ว; iterate ผ่าน /autoresearch หรือ /skill-builder เมื่อต้องการ | Completed |
| 2026-05-01 | Gemini integration lives in isolated `codex-gemini-sidecar/` workspace | Bond ต้องการใช้ Gemini Pro ให้คุ้มแต่ไม่อยากเสี่ยงกับโฟลเดอร์หลัก; isolated workspace ทำให้ทดลองและ rollback ง่าย | Implemented |
| 2026-05-01 | Shared AI architecture = Claude/Codex owner, Gemini CLI sidecar = `lite` / `research` / `challenge` / `corpus` | Browser automation เปราะและกิน effort; CLI broker path เสถียรกว่าและให้ทั้ง Claude กับ Codex ใช้ flow เดียวกัน | Implemented |
| 2026-05-01 | Headless Gemini sidecar defaults to cached Google AI Pro login + `GOOGLE_GENAI_USE_GCA=true` + trusted workspace wrapper | Non-interactive Gemini CLI บนเครื่องนี้ต้องมี auth/trust env เพิ่ม แม้ login จะ cache แล้ว; wrapper แก้ pain point โดยไม่ต้องพึ่ง Gemini API billing | Implemented |
| 2026-05-01 | Archive Codex strategic proposal for Second Brain as reference only — not committed execution | Bond liked Codex's workflow-intelligence reframe and wanted it preserved in-project, but explicitly does not want it treated as the real plan yet | Archived |
| 2026-05-03 | แก้โครงสร้างธุรกิจห้องพัก: Bond เปิดแข่งกับแม่ (ไม่ใช่ co-manage) | ความเข้าใจเดิมผิด — Bond จ่ายแม่ 500/คืน Bond ได้ส่วนต่าง; long-term goal: บริหารแทนแม่ 400/คืน | Corrected |
| 2026-05-03 | Impact Arena Condo Priority: OTA fix → Line OA channel → Exhibitor outreach | Theory of Constraints: fixing Booking ranking ก่อนเป็น leverage สูงสุด; direct channel ตามหลัง; social media = Phase 3+ | Decided |
| 2026-05-03 | C5 review score target 8.5+ achievable; C3/C4/C9 capped ~7.8 จนกว่าจะ deep clean | C5 score จริง = 7.7 (ไม่ใช่ 8.0); ปัญหา physical (แมลงสาบ/cleanliness) ใน C3/C4/C9 block score ขึ้น | Decided |
| 2026-05-03 | Auto Skill Log rule: ทุก skill ต้อง append ผลลงใน projects/[project]/log.md ทันที | Bond ต้องการ project folders ที่ compound intelligence ข้าม sessions — ไม่ต้อง re-derive context ซ้ำ | Implemented |
| 2026-05-03 | tasks/todos.md = Master Todo List สำหรับทุกเรื่องในชีวิต Bond; load ทุก session ผ่าน CLAUDE.md | Bond ต้องการ single source of truth ที่ Claude รู้ตลอด; sync deadline items กับ Google Calendar | Implemented |
| 2026-05-03 | Google Calendar integration: ใช้ Work calendar สำหรับ business items; create events ผ่าน MCP | Confirmed working — 9 events สร้างสำเร็จ; action reminders มี popup notification | Implemented |
| 2026-05-03 | THAIFEX pipeline: Bond ทำ Phase 1 เอง (register + pull list); Claude ทำ Phases 2-5 | Exhibitor list ต้อง login — ไม่สามารถ automate Phase 1 ได้; แต่ Claude handle research/messages/tracking ได้หมด | Decided |
| 2026-05-03 | /consult: ห้ามเลือก dual notebook โดยไม่ได้รับ authorization จาก Bond | Bond corrected: routing table เป็น guideline สำหรับ Claude decide เอง แต่ถ้าจะ dual-query ต้องแจ้งก่อน | Corrected |
| 2026-05-04 | Second Brain beachhead validation = Bond himself + KU engineering students ก่อน; commercial beachhead = Thai regulated professionals ตาม ≥ Q3 2026 | KU validation: zero CAC + feedback loop สมบูรณ์; legal professionals ต้องการ credibility + sales cycle ที่ Bond ไม่มีใน 3 เดือน | Decided |
| 2026-05-04 | Primary moat = data gravity ใน Second Brain (compounding Markdown knowledge) ไม่ใช่ local/Ollama | Local/Ollama เป็น commodity ในปี 2026 (Block goose, Cline, OpenCode ทำแล้ว); data gravity = ยิ่งใช้นาน ย้ายยิ่งเจ็บ คือ durability moat จริง | Decided |
| 2026-05-04 | Auto-capture (conversation → Markdown อัตโนมัติ) = critical feature ไม่ใช่ nice-to-have | Manual input = cold start problem = churn สูง; Notion/Roam/Obsidian มีปัญหานี้ทุกตัว; ถ้าไม่แก้ = product fails แม้ vision ถูก | Decided |
| 2026-05-04 | Local/on-premise = entry advantage (18-24 เดือน) ไม่ใช่ permanent moat | Microsoft Azure Local + Apple Intelligence + Google Gemma เริ่ม enter local space; ต้อง build vertical domain lock-in ให้ทันก่อน window ปิด | Decided |
| 2026-05-04 | 90-day roadmap: M1=build+dog-food (Bond ใช้เอง), M2=validate+recruit (5 KU users 14 วัน + pitch co-founders), M3=lock+submit (Senior Project + NIA grant) | Sequential validation ลด risk; ใช้ working demo แทน slides ในการ pitch ทีม | Decided |
| 2026-05-05 | Merge "AI ONE FOR ALL" concepts เข้า Second Brain vision: เพิ่ม AI Communication Bridge + AI Network + Knowledge Inheritance เป็น core differentiators | Root cause ที่แท้จริงของ problem = knowledge อยู่ในหัวคน ไม่ใช่ระบบ; AI Communication Bridge (AI brief ทีมแทนเจ้าของ) คือ differentiator ที่ยังไม่มีใครสร้าง; ตัดออก: HR AI สัมภาษณ์, CEO AI, Marketing trend AI — เป็น commodity หรือ legal landmine | Decided |
| 2026-05-05 | เปลี่ยน Second Brain จาก local-first → Online | Local ทำ multi-user team collaboration ไม่ได้จริง; team use case คือ core value prop; moat เปลี่ยนจาก privacy → AI-to-AI protocol + data gravity | Decided |
| 2026-05-05 | Beachhead เปลี่ยนจาก "KU admin office" → "KU Senior Project teams" | Bond คือ target user (zero CAC); pain ชัด (handoff, decision loss, knowledge decay); 3-5 ทีม = 15-30 users สำหรับ validate | Decided |
| 2026-05-05 | Positioning locked: "Notion AI อ่านที่คุณเขียน — Second Brain รู้ที่คุณทำ แล้วทำแทน" | Active behavioral intelligence vs passive document knowledge — ต่างกันพื้นฐาน ไม่ใช่แค่ feature | Decided |
| 2026-05-05 | Agent Harness: re-engineer architecture (ไม่ copy code) บน Claude SDK + MCP foundation | IP-safe; build unique layer (org memory, AI-to-AI protocol, capture engine) เท่านั้น; infrastructure ใช้ที่มีอยู่แล้ว | Decided |
| 2026-05-05 | Privacy model: behavioral data = shareable; personal data = private; ทุก query logged และ owner เห็นได้ | Enterprise sales ต้องการ privacy story ที่ชัด; reframe จาก surveillance → employee-controlled knowledge sharing | Decided |
| 2026-05-06 | Second Brain: GO verdict ยืนยัน — ตลาดมีจริง, Thailand zero competitor, cost structure viable | /research-brief + /scenario-planner + /consult สามตัวอิสระ converge ที่ GO; white space อยู่ที่ AI-to-AI personal collab layer ที่ยังไม่มีใครทำ | Decided |
| 2026-05-06 | KU pilot retention metric เปลี่ยนจาก "60% หลัง 14 วัน" → "≥ 3/5 users กลับมาเองใน Day 3 โดยไม่มีคนทวง" | D7 benchmark ของ productivity app = 10-18%; 60% เป็นไปไม่ได้สำหรับ app ใหม่ไม่มี viral loop; Day-3 intrinsic return = PMF signal ที่ realistic และ actionable | Decided |
| 2026-05-06 | Network effect ของ Second Brain = "lagged" ไม่ใช่ instant อย่าง LINE — ต้อง hand-select และ onboard user ทีละคนในช่วงแรก | LINE โตเพราะ add เพื่อนได้ใน 10 วินาที; Second Brain ต้อง setup cold start ก่อนมีคุณค่า; ห้ามพึ่ง viral growth จาก KU pilot | Decided |
| 2026-05-06 | KU pilot sequence ล็อก: Week 1-2 = Solo AI companion MVP → Week 3 = Collab layer (เฉพาะถ้า Day-3 pass) → Post-KU = B2B Team Brain | 77% of users ร่วงภายใน 3 วันแรก ก่อน collab feature แสดงค่าได้; solo retention คือ riskiest assumption #1 | Decided |
| 2026-05-06 | Model strategy: DeepSeek V3 ($0.14/M input) สำหรับ bulk tasks + Claude Sonnet 4.6 ($3/M) สำหรับ high-reasoning เท่านั้น | DeepSeek V3 ถูกกว่า Claude 21× input, 54× output; cost/user ≈ $0.04/วัน สำหรับ 30 users = viable freemium; MCP abstraction layer ป้องกัน lock-in | Decided |
| 2026-05-06 | PDPA architecture: "Bring Your Own Brain" — แต่ละ user เป็น Controller ของตัวเอง; sharing เป็น explicit opt-in ต่อ project; Skills แชร์ได้ by default, personal context แชร์ไม่ได้ | PDPA Privacy-by-design + PDPC 2025-2026 AI guidelines; ทำให้ B2C ไม่ต้อง DPIA; B2B employer ไม่ใช่ Controller | Decided |
| 2026-05-06 | Build บน A2A open standard (v1.0 stable เม.ย. 2026, 150+ orgs, SDK 5 ภาษา) — ห้ามสร้าง protocol เอง | A2A v1.0 พร้อมใช้แล้ว; สร้างเองคือ Webvan trap (เสีย time build infra แทน product); moat ไม่ได้อยู่ที่ protocol แต่ที่ shared memory ที่สะสม | Decided |
| 2026-05-06 | Architecture direction: Agent Harness (ไม่ใช่ Software wrapper) — ศึกษา Claude Code open source สำหรับ design inspiration โดยไม่ copy code | Bond ประสบการณ์ตรง: Gemini ผ่าน Anti-Gravity harness ดีกว่า plain Gemini มากมาย; agent harness ให้ tool calling, planning loop, context management ที่ software wrapper ทำไม่ได้ | Decided |
| 2026-05-06 | Pricing tiers: Free (30 interactions/day, DeepSeek only) / Starter 199 THB/เดือน / Pro 699 THB/เดือน (~$20 เทียบเท่า Claude Pro) / Team 1,490 THB/seat/เดือน | Bond ต้องการ main tier ที่ ~$20 (premium positioning เทียบ ChatGPT Plus/Claude Pro) แต่เก็บ entry tier ไว้สำหรับ accessibility; Starter 199 THB เพื่อเข้าถึงนักศึกษา/คนที่อยากลองก่อน | Decided |
| 2026-05-06 | Tech stack: Python + bare Claude SDK (tool loop เขียนเอง ~100 lines) + MCP SDK + A2A SDK (Google, v1.0) + rapidfuzz for MVP search → Chroma + MiniLM ใน Week 3+ | Simplest that works; Bond ควบคุมได้เต็ม; ไม่ต้องเรียน framework ใหม่; a2a-sdk พร้อมใช้แล้ว | Decided |
| 2026-05-08 | Three-brain architecture: Claude = orchestrator (spec + decisions), Codex = heavy implementation >50 lines + reviewer, Gemini = vision + large file read | Claude หมด token เร็ว; Codex/Gemini burn quota ของตัวเอง; token conservation = Claude ไม่ write code inline เลยถ้า >50 บรรทัด | Implemented |
| 2026-05-08 | Codex subscription: Solo Plus 699 THB (ไม่ shared) แนะนำสำหรับ Second Brain | IP ของ Second Brain ห้ามผ่าน shared 10-person account; Plus quota เพียงพอสำหรับ reviewer + implementation role | Pending (Bond ยังไม่ได้ subscribe) |
| 2026-05-08 | Claude Code /plugin commands ใช้ได้เฉพาะ CLI terminal ไม่ใช่ VSCode extension | /plugin ไม่มีใน VSCode extension environment; ต้องรัน `claude` ใน terminal แล้ว install จากนั้น | Noted |
| 2026-05-08 | Agent harness architecture = 5-layer modular prompt assembly (identity / policy / runtime context / tool descriptions / memory+skills overlay) — ห้าม hardcode ลง prompt เดียว | ยืนยันจาก Claude Code internal extraction; LangGraph/OpenHands/Pydantic AI ต่างใช้ pattern นี้; 5-layer = flexibility + context cost control ในเวลาเดียวกัน | Decided |
| 2026-05-09 | KU Pilot redesign: ใช้ business seniors (consulting) + ธุรการ Bond (B2B) แทน KU classmates | KU classmates = captive audience; กลับมาเพราะ social obligation ไม่ใช่ value; seniors ทำ consulting = real unbiased use case | Decided |
| 2026-05-09 | Cold start fix: progressive disclosure ผ่าน conversation แทน forced onboarding form | 12-question form = profile ธรรมดา ไม่ใช่ "brain"; AI ดึงบริบทออกผ่านการใช้งานจริง = natural + ไม่ friction | Decided |
| 2026-05-09 | A2A ตัดออกจาก MVP — solo brain เสร็จ → วัด Day-3 → A2A ถ้าผ่านเท่านั้น | A2A ใน Week 3 ของ solo developer = unrealistic; ต้องการ mature brain ทั้งสองฝั่ง; early users ไม่ได้ซื้อ collab feature | Decided |
| 2026-05-09 | Build timeline: 5 สัปดาห์ปิดเทอม (9 พ.ค.–14 มิ.ย. 2026) แทน "3 weeks" เดิม | Bond ปิดเทอมถึงกลาง มิ.ย.; realistic estimate สำหรับ solo non-SE developer คือ 2-4 เดือน แต่ 5 สัปดาห์ full-time เป็นไปได้ถ้าลด scope | Decided |
| 2026-05-09 | Bootstrap strategy: Phase A = hook memory เข้า Claude Code ก่อน; Phase B = standalone Streamlit web app | ไม่ต้องสร้าง "Second Brain เพื่อสร้าง Second Brain"; ใช้ Claude Code + memory layer ระหว่าง build จริง → dogfood ตั้งแต่วันแรก | Decided |
| 2026-05-09 | AI-to-AI protocol: HTTP peer endpoint ก่อน → A2A SDK v1.0 เมื่อต้องการ ecosystem compatibility | HTTP = 1-2 วัน Bond เข้าใจทุกบรรทัด; A2A = 1-2 สัปดาห์; MVP ไม่ต้องการ ecosystem compatibility ตอนนี้ | Decided |
| 2026-05-09 | Memory stack: Markdown (truth) + SQLite FTS5 (search) → Chroma/MiniLM เฉพาะ v2+ | Obsidian = app สำหรับมนุษย์ ไม่ใช่ backend; Vector DB เป็น source of truth = ไม่ debug ได้, stale memory แพร่เร็ว; Markdown human-inspectable | Decided |
| 2026-05-09 | Architecture: Python จาก scratch ~200 lines core harness (ไม่ fork opencode/OpenHands) | Bond ต้องเข้าใจทุกบรรทัดเพื่อ debug; fork = dependency ที่ Bond ไม่ control; patterns จาก research ใช้ได้โดยไม่ต้อง copy code | Decided |
| 2026-05-09 | Phase 0 model: Qwen 3.6 หรือ DeepSeek V3 ฟรีบน OpenRouter | เห็น contrast raw model vs harness ได้ชัดสุด; ประหยัดค่าใช้จ่ายระหว่าง learning phase; proof ว่า harness value ≠ model quality | Decided |
| 2026-05-09 | Model strategy: model-agnostic ผ่าน env var (MODEL_PROVIDER / MODEL_ID / API_BASE) — ไม่ lock DeepSeek เป็น default | DeepSeek เป็นแค่ตัวอย่าง; harness ที่ดีทำให้ทุก model ทำงานได้ดีขึ้น; flexibility ต่ออนาคต | Decided |
| 2026-05-09 | Second Brain repo: ~/Documents/Code/SecondBrain/ แยกจาก MyBrain | production code ไม่ควรอยู่ใน planning workspace; แยก concern ตั้งแต่ต้น | Decided |
| 2026-05-09 | B2B brain model: personal brain ติดตาม user ออกจาก org; org brain ยังอยู่ใน system | resolves "institutional memory ไม่ match กับ KU students ที่อยากจบแล้วไป"; ทุกคนเอา brain ส่วนตัวไป; org รักษา shared decisions ไว้ | Decided |
| 2026-05-09 | Dreaming (Anthropic 5 พ.ค. 2026): Second Brain ต้องสร้าง version ของตัวเอง (model-agnostic) | Anthropic Dreaming ผูกกับ Managed Agents platform; ไม่ได้ใช้ bare API; Second Brain's differentiation: cross-user collab + model-agnostic dreaming | Decided |
| 2026-05-09 | Code integration: Second Brain = orchestrator ที่ใช้ Claude Code เป็น sub-agent; ไม่ใช่ plugin | Plugin = gimmick + ขึ้นอยู่กับ competitor; orchestrator = Claude Code เป็น tool ที่ Second Brain เรียกใช้ = Second Brain รักษา control | Decided |
| 2026-05-08 | Top 3 build order ล็อก: (1) memory-as-files + retrieval gate + session autosave, (2) tool runner + hook bus + hard budgets, (3) Claude proposer → Gemini challenger (เฉพาะหลัง eval loop พร้อม) | ลำดับนี้มาจาก deep research synthesis; ถ้า authoritative memory ยัง unclear ทุกอย่างที่ตามมา debug ยาก; hook bus ต้องเป็น core primitive ไม่ใช่ addon; dual-LLM เพิ่ม complexity ก่อน eval loop = วัดไม่ได้ | Decided |
| 2026-05-08 | Pydantic AI = primary reference harness สำหรับ Second Brain; OpenHands = secondary reference สำหรับ real-world agent runtime | Pydantic AI: type-safe, composable, official harness layer แยกออกมา, closest to "build your own clean harness in Python"; OpenHands: production-grade coding agent runtime, context condenser, stuck detector pattern | Decided |
| 2026-05-08 | Cross-brain query = read-only tool เท่านั้น (`query_user_brain(user_id, question)`) — ห้าม write access ข้าม brain ใน v1 | "shared write = shared corruption" pattern พบซ้ำในทุก harness ที่ mature; ACL ชัดเจนต้องมาก่อน write permission | Decided |
| 2026-05-09 | Second Brain = OS Platform (ไม่ใช่แค่ productivity tool) — Skill Store + Knowledge Store + Creator Ecosystem | iOS App Store analogy: platform moat = ecosystem ที่ competitor copy feature ไม่ได้ แต่ copy network effect ไม่ได้; creator incentive (revenue share 70/30) แก้ cold start; Knowledge Marketplace ยังไม่มีใครทำจริง | Decided |
| 2026-05-09 | Foundation strategy: ใช้ open-source Claude Code clone (commercially licensed) + Hermes + agent harness อื่น เป็น base — supersedes "Python scratch 200 lines" | Battle-tested open-source foundation = faster path to Second Brain's unique layer; moat อยู่ที่ memory system + skill ecosystem ไม่ใช่ agent loop mechanics; clone อยู่ใน scratch/ (ต้องตรวจสอบ license ก่อน build) | Decided |
| 2026-05-11 | Haiku 4.5 viable สำหรับ analytical tasks — ไม่จำเป็นต้อง default Sonnet ทุกงาน | Bond ทดสอบ empirically: Haiku 4.5 (Compet 1) vs Sonnet 4.6 High (Compet 2) ด้วย prompt เดียวกัน — Haiku ทำ strategic breadth ได้ดีกว่า; Sonnet ทำ technical depth ได้ดีกว่า; ทั้งสองมี unique blind spots ต่างกัน; การ merge output จาก 2 models = coverage ที่สมบูรณ์กว่า single model เสมอ | Decided |
| 2026-05-12 | Foundation ยืนยัน: GPT-AGI/Clawd-Code (MIT, Python 100%) = base สำหรับ Second Brain build tool | Working Python harness มี 30+ tools, skill loader, multi-provider, compaction อยู่แล้ว; ประหยัด months ของ building from scratch; cloned ลง scratch/clawd-code/ แล้ว | Confirmed |
| 2026-05-12 | Build approach: 4 AI agents ทำงาน parallel/sequential — AI 1A (Foundation), AI 1B (Commands/Tools/Wiring, รอ 1A), AI 2 (Memory/Skills, parallel กับ 1A), AI A (Closer: review+wire, รอทุกตัว) | ~17,000 LOC เกินที่ AI ตัวเดียวจะทำได้; แบ่ง ownership boundary ชัดเจนป้องกัน conflicts; prompts เขียนไว้ใน .claude/plans/ ครบแล้ว | Decided |
| 2026-05-12 | Production Second Brain ≠ Claude Code harness: Production = Chat Mode agent (ไม่ใช่ IDE/CLI); Clawd-Code harness = internal build tool เท่านั้น | Second Brain ไม่ใช่ coding assistant; end users เห็นแค่ ChatGPT-like chat UI; harness ใช้ internally สำหรับ build/maintain ระบบผ่าน MCP ทีหลัง | Clarified |
| 2026-05-12 | Chat UI ก่อน, MCP ทีหลัง | Chat UI validate core UX + give immediate value; MCP เป็น integration layer ที่ทำได้เมื่อ Chat stable; MCP ไม่ใช่ gimmick แต่เป็น Phase 2+ | Decided |
| 2026-05-12 | Code Repository concept: ทุก code/simulation/exercise ที่ AI สร้างใน session → persist เป็น searchable asset (open หรือ private mode); AI ดึงจาก repository ก่อนสร้างใหม่ | Compound knowledge: Thermo simulation สร้างครั้งแรก → ครั้งถัดไป AI adapt เป็น CFD สำหรับรถสปอร์ต → ไม่ทำซ้ำ; school/org version = private asset ที่เป็น IP ของ org | New Feature |
| 2026-05-12 | Memory temporal schema: ทุก memory unit ต้องมี created_at, last_validated_at, expires_at (optional), confidence (0-1) | ไม่มี temporal data = ไม่รู้ว่าความจำไหนเก่า/ใหม่/ยังใช้ได้; expires_at สำหรับ deadline/events; confidence ลดเมื่อมีข้อมูลใหม่ขัดแย้ง; inspired by Letta/MemGPT + MemoryOS | Architecture |
| 2026-05-12 | Memory architecture: 5 foundational layers — Memory Substrate (Markdown+SQLite FTS5+JSONL), 4-tier system (Core/Episodic/Semantic/Procedural), Skill System, MCP Interface, Agent Loop | รากฐานไม่ใช่แค่ markdown; SQLite FTS5 เป็น must-have สำหรับ temporal query; 4 tiers จาก Letta + MemoryOS; MCP = integration layer สำหรับ external tools | Designed |
| 2026-05-12 | Second Brain = OS ที่ enhance AI model ใดก็ได้ (DeepSeek, ChatGPT, Gemini, Claude) — ไม่ใช่แค่ productivity tool | เมื่อ model ผ่าน Second Brain harness: thinking ดีขึ้น, คำตอบแม่นขึ้น, มี context ที่สะสม; Skill Store = ขายสิ่งที่ทำให้ AI เก่งขึ้น (MCPs, context packs, skills, training sets) | Vision Confirmed |
| 2026-05-12 | VSCode ยังคงใช้ต่อไปจนกว่า Second Brain Chat UI ดีกว่าจริงๆ — ไม่รีบย้าย | Bond จ่าย $20/เดือน Claude Code subscription ที่ให้ value ชัดเจน; ย้ายเมื่อ: auto-memory ทำงาน + UI ไม่ต้อง terminal + context persist ข้าม session; Desktop app ใช้ Tauri (ไม่ใช่ Electron) | Decided |
| 2026-05-12 | 3-product vision: (1) Second Brain core, (2) NotebookLM clone, (3) Code Repository — แยกกัน integrate ผ่าน Skill Store | Second Brain ไม่ใช่แค่ productivity tool; NotebookLM clone เป็น knowledge corpus layer; Code Repository เป็น compound knowledge ที่ AI-generated | Decided |
| 2026-05-12 | Phase 1 COMPLETE — AI A Closer verified all wiring: memory injection ✅, post-sampling hooks ✅, memory extraction ✅, 22 commands bridged ✅, 271 tests pass ✅ | ตรวจพบว่า Explore agent ครั้งแรก miss ว่า AI A Closer ได้ add methods เพิ่ม (line 105, 1301, 1401-1402) ให้ repl/core.py แล้ว; Phase 1 done สมบูรณ์ | Confirmed |
| 2026-05-12 | Phase 2 scope = Core เท่านั้น — NO Streamlit UI; SecondBrainSession + model_router + bug fixes | Chat UI ต้องมี working core ก่อน; UI บน broken core = waste; Streamlit ย้ายไป Phase 3 | Decided |
| 2026-05-12 | SecondBrainSession (src/second_brain/session.py) = headless backend สำหรับ UI ใดก็ได้ในอนาคต | ไม่ import ClawdREPL (ผูกกับ terminal I/O + sys.exit); reuse run_agent_loop, brain_engine, providers โดยตรง; return TurnResult struct แทน print; pattern นี้ทำให้ Streamlit/Next.js/Tauri plug in ได้ไม่ต้องแตะ core | Decided |
| 2026-05-12 | Model router Phase 2 = env-var stub (SECOND_BRAIN_PROVIDER); Phase 3 = complexity-based routing Haiku→Sonnet→Opus | Phase 2 focus = correctness ไม่ใช่ cost optimization; router architecture พร้อมแล้วเพื่อ extend ใน Phase 3; complexity score (_complexity_score stub) เขียนไว้ pre-built | Decided |
| 2026-05-12 | Strip 15 IDE tools จาก defaults.py — 29 chat tools เหลือ (WebSearch, WebFetch, MCP, Skills, Tasks, Cron, Team, Agent, Brief ฯลฯ) | Second Brain = chat product ไม่ใช่ IDE; BashTool/FileRead/Edit/Glob/Grep/LSP/Worktree/REPL/PowerShell/Notebook ไม่ใช่ use case ของ Second Brain | Implemented |
| 2026-05-12 | 4-repo design patterns (mem0/knowledge-base-server/AIngram/ReMe) ต้องระบุใน Codex prompt ชัดเจน | Codex อาจ bypass patterns โดยไม่รู้ตัวถ้าไม่ explicit; ทุก pattern embed ใน brain_engine แล้ว — Codex ต้องเรียกใช้ function เหล่านั้น ไม่ใช่ re-implement | Architecture |
| 2026-05-12 | ROADMAP.md สร้างที่ projects/second-brain/ROADMAP.md เป็น master plan แทน BUILD.md ที่ถูกลบ | 5-phase roadmap: Harness → Core → Web/Pilot → Platform → Open; ให้เห็นทิศทางชัดว่าแต่ละ phase ทำอะไรและทำไม | Done |
| 2026-05-12 | Phase 2 Core COMPLETE — 8 files, 271 tests pass, all validations pass | Codex built model_router + SecondBrainSession + fixed /status bug + updated 3 test files; ทุก architectural constraint ผ่านการตรวจ (init_db ✅, MemoryStats 4 fields ✅, finally restore ✅, run_awaitable_sync ✅, no asyncio.run ✅) | Done |
| 2026-05-13 | Second Brain web code execution ต้องเป็น preview-only by default; process languages ต้อง gated ด้วย `SECOND_BRAIN_ENABLE_UNSAFE_CODE_RUN` | Chat product สำหรับ non-tech users ห้ามมีปุ่ม Run ที่ execute Python/JS/shell โดยตรงผ่าน local web server; preview HTML/SVG/CSS พอสำหรับ UX ตอนนี้ ส่วน code execution จริงต้องรอ sandbox/permission model | Implemented |
| 2026-05-13 | Phase 3 web fixes ต้อง prioritize retention/trust/data ownership เหนือ checklist feature | เป้าหมายไม่ใช่ทำ dropdown/menu ให้ครบ แต่คือ product ที่ Bond ใช้แทน Claude Desktop ได้จริง: memory/project correctness, safe defaults, no raw technical leaks, UI states ที่ไม่รู้สึกเป็น toy | Decided |
| 2026-05-13 | Second Brain launch UI ห้ามมี dead controls — ปุ่มบนหน้าจอต้องทำงานจริง มี fallback state หรือถูกถอดออก | ปุ่ม search/voice/feedback/regenerate ที่กดแล้วไม่มีผลทำให้ product รู้สึกเป็น prototype ทันที; รอบนี้ wire search/voice/feedback และถอด regenerate จนกว่าจะมี backend semantics ที่ถูกต้อง | Implemented |
| 2026-05-13 | Frontend production build ต้อง split chunks แทนก้อนเดียวใหญ่ | Initial JS chunk 528–600 kB ทำให้ first load หนักและมี Vite warning; manual chunks แยก React/Markdown/Motion/icons + lazy views ลด app chunk เหลือ ~65 kB | Implemented |
| 2026-05-13 | Mobile sidebar ต้องเป็น drawer ไม่ใช่ `display:none` | Mobile เดิมซ่อน navigation ทั้งหมด ทำให้ user ไป settings/projects/new chat ไม่ได้; launch UI ต้องมี primary navigation บนมือถือเสมอ | Implemented |
| 2026-05-13 | Composer `+` menu เป็น primary action hub; Deep Research ไม่ควรเป็นปุ่มหลัก | Bond ต้องการ Deep Research เจอเมื่อกด `+` เท่านั้น; ปุ่มหลักเยอะทำให้ composer ดู toy และเพิ่ม dead-surface risk | Implemented |
| 2026-05-13 | Voice input ซ่อนหลัง feature flag จนกว่าจะมี voice pipeline ที่เชื่อถือได้ | Web Speech API ไม่เสถียรพอสำหรับ launch และปุ่มไมค์ที่กดแล้วไม่ทำงานทำลาย trust ทันที; เปิดได้ด้วย `localStorage.secondBrain.voiceInput=1` เฉพาะทดลอง | Implemented |
| 2026-05-13 | Project = goal workspace ไม่ใช่ Project Info form | Bond ต้องการให้ AI/Project ช่วยรักษาเป้าหมายต่อเนื่องข้ามแชท; default view จึงต้องเป็น Goals + Project Chats ส่วน info ไปอยู่ใน edit modal | Implemented |
| 2026-05-13 | Project chat list ต้องใช้ endpoint เฉพาะ project sessions ไม่ใช่ filter `/api/sessions` ใน frontend | Sidebar นับ project sessions จาก SQLite แต่ ProjectView filter จาก recent sessions แล้วหลุดได้; mismatch นี้ทำให้ผู้ใช้รู้สึกว่าข้อมูลหาย | Implemented |
| 2026-05-13 | WebSearch mode ต้องเป็น turn option ไม่ใช่ข้อความ `[web-search]` ที่ผู้ใช้เห็น | Prefix ในกล่องแชททำให้ product ดูเหมือน prompt hack; frontend ควรส่ง hidden instruction เข้า request แต่ user bubble ต้องสะอาด | Implemented |
| 2026-05-13 | Project และ Memory ต้องรวมเป็น Academic Goal Workspace | Research เรื่อง self-regulated learning + Zettelkasten ชี้ว่า academic workflow ต้องมี goal, evidence, reflection, project notes ไม่ใช่ chat history อย่างเดียว | Implemented (frontend foundation) |
| 2026-05-13 | Ghost mode + system theme + Open Source entry เป็น part of Claude-class chat UX | ผู้ใช้ต้องควบคุม memory privacy ได้แบบ Temporary Chat, appearance ต้อง match system by default แบบ native app, และ Code Repository ต้องเริ่มมี surface ใน UI เพื่อสะท้อน Second Brain platform vision | Implemented |
| 2026-05-13 | Every successful chat turn ต้องถูกจับเป็น episodic memory ที่ค้นคืนได้หลังปิดแชท แต่ห้าม spam `MEMORY.md` | Bond ต้องการให้ Second Brain “จำได้ทุกอย่าง”; solution ที่ production-safe คือ session JSON เก็บ transcript แบบ atomic, episodic Markdown+SQLite เก็บ searchable recall, durable extractor promote เฉพาะ fact/decision สำคัญเข้า memory prompt | Implemented |
| 2026-05-13 | Auto Dream ต้องเป็น system-triggered consolidation ไม่ใช่ manual button | Bond ไม่ต้องการให้ผู้ใช้กดจัดการ memory เอง; หลัง turn สำเร็จ web session จะเช็ค gate เองเมื่อ `CLAWD_AUTO_DREAM=1`, มี persistent last-consolidated marker, และ fallback จะ consolidate จาก episodic memories เท่านั้นเพื่อไม่ดูด ghost-mode raw session history | Implemented |
| 2026-05-13 | Memory Control Plane ต้องมี inspector/provenance/event history ไม่ใช่แค่ search list | ถ้า memory จำได้ทุกอย่างแต่ตรวจสอบไม่ได้ ผู้ใช้จะไม่ trust; รอบนี้เพิ่ม `/api/memory/{id}`, parsed source session/captured time, event history, และ UI Inspect modal เพื่อให้ Bond เห็นว่า memory มาจากไหน/เปลี่ยนเมื่อไร | Implemented |
| 2026-05-13 | Auto Dream consolidation ต้องใช้ agent/model เป็น quality layer เหนือ heuristic fallback | heuristic extraction ดีพอสำหรับ bootstrap แต่ไม่พอสำหรับ semantic/procedural memory ระยะยาว; Auto Dream ตอนนี้ส่ง episodic memories เข้า prompt แบบ JSON contract แล้ว store durable memories จาก agent response เมื่อมี `agent_caller` | Implemented |
| 2026-05-13 | Project memory extraction ต้องติด scope ของ project ตั้งแต่ session runtime | Project = goal workspace; decision/reflection/next action จาก project chat ต้อง searchable เฉพาะ project นั้นได้ ไม่ปะปน global memory; `SecondBrainSession` จึงถือ active project id และส่ง scope เข้า extractor/episodic capture | Implemented |
| 2026-05-13 | Context compaction ต้อง preserve fresh tail เสมอ | OpenHands/PicoClaw pattern ชัดว่า compact ที่ลบ recent task state ทำให้ agent ลืมงานทันที; compact service จึง summarize เฉพาะ history เก่าและ keep recent messages verbatim | Implemented |
| 2026-05-13 | Second Brain ต้องมี token budget control ก่อน inject memory | เดิม `refresh_context` + session memory + `MEMORY.md` ถูก append ซ้ำ ทำให้ context bloat; เพิ่ม context budget estimator และให้ memory block ถูก trim ตาม pressure ก่อนเข้า agent loop | Implemented |
| 2026-05-13 | Compact ต้องเป็น skill ที่ invoke ได้ใน web/agent harness | Bond ขอ skill compact; เพิ่ม default `/compact` skill สำหรับ Second Brain และ bundled `compact` skill สำหรับ harness เพื่อให้ AI มี workflow เดียวกันในการ handoff/context compression | Implemented |
| 2026-05-13 | Second Brain web backend ต้องมี API contract เดียวผ่าน `web_api.py` | `web_server.py` กับ `fastapi_app.py` เคยมี route mapping ซ้ำและ behavior drift เช่น validation/404/SSE error/ghost mode; ย้าย dispatch, JSON parsing, CORS origin policy, SSE error frame เข้า source of truth เดียว แล้วให้ HTTP server และ FastAPI gateway เรียกผ่าน contract เดียวกัน | Implemented |
| 2026-05-13 | FastAPI gateway ต้องมี TestClient runtime gate ไม่ใช่แค่ unit/import test | TestClient จริงเจอ bug ที่ unit test ไม่เห็น: `from __future__ import annotations` ทำให้ FastAPI มอง `Request` เป็น query param เพราะ import อยู่ใน local scope; แก้แล้วและเพิ่ม FastAPI-only tests แบบ skip เมื่อ dependency ไม่พร้อม | Implemented |
| 2026-05-13 | Memory writes/retrieval ต้องผ่าน Memory Control Plane policy แทน caller ใส่ metadata เอง | Memory เดิมมี `confidence/importance/scope` แต่ caller กระจัดกระจายและ project chat ดึงเฉพาะ project-scope จนลืม global preference ได้; เพิ่ม `memory_policy.py`, `memory_kind`, provenance fields, policy defaults, importance-aware ranking, และ retrieval ที่รวม global + project-scoped memories | Implemented |
| 2026-05-13 | Memory conflict ต้องเข้าคิว explicit resolution ไม่ใช่ auto-overwrite หรือ active duplicate | ถ้าความจำใหม่ negates/corrects active memory เดิม ระบบต้อง queue ใน `memory_conflicts` พร้อม proposed content/reason/similarity แล้วรอ resolve keep/replace/merge; ลดความเสี่ยง AI จำสองความจริงขัดกันใน prompt | Implemented |
| 2026-05-13 | Memory conflict queue ต้องมี UI review path ไม่ใช่แค่ API | ถ้า conflict ถูก queue เงียบ ๆ ผู้ใช้จะไม่รู้ว่าความจำค้างอยู่และ AI จะไม่ evolve trust; เพิ่ม Needs Review ใน Memory Browser พร้อม keep current/use proposed/merge และ badge ใน Memory Panel | Implemented |
| 2026-05-13 | Frontend quality gate ต้องเป็น green ก่อนถือว่า ship ได้ | `npm run lint` เคยแดงจาก React compiler rules ที่ไม่เหมาะกับ data-fetching pattern ปัจจุบันและ `any` ใน Markdown code renderer; ปรับ lint policy เฉพาะกฎที่เป็น false-positive/fast-refresh dev-only และแก้ type จริงจน lint/build ผ่าน | Implemented |
| 2026-05-13 | ก่อน Session 3 ต้อง freeze `brain_engine.py` ให้เล็กลงและมี module boundary ชัด | `brain_engine.py` บวมถึง 1601 บรรทัดและรวม models/schema/markdown/utils/write/search/conflict ในไฟล์เดียว เสี่ยงมากถ้าต่อ Compact + Context Ledger; แยก `memory_models.py`, `memory_utils.py`, `memory_schema.py`, `memory_markdown.py` โดยรักษา public contract เดิม | Implemented |
| 2026-05-13 | Session 3 Context Ledger ต้องเป็น audit trail แยกจาก raw memory/chat content | Compact/context ที่ไม่มีบัญชีว่าอะไรถูก inject/trim/compact debug ไม่ได้เมื่อ agent ลืมงาน; เพิ่ม JSONL per-session ledger ที่เก็บ token/action/reason/hash ไม่เก็บ raw memory, นับ runtime prompt/tool schemas ใน budget, ผูกกับ turn context, auto compact, manual compact, SSE payload, และ API `/api/context/ledger` | Implemented |
| 2026-05-13 | Context Ledger ต้องมี user-facing Context Inspector ไม่ใช่แค่ backend API | ถ้า context decision อยู่แต่ใน JSONL ผู้ใช้ยังไม่รู้ว่า memory/tool/schema อะไรถูก include/trim/compact; เพิ่ม Context Inspector ใน Memory Panel แสดง token used/saved, action chips, reason และ latest ledger โดยไม่เปิด raw private content | Implemented |
| 2026-05-13 | Tool result bloat ต้องถูก compact ก่อนกลับเข้า model history | Tool output ใหญ่จาก search/browser/file tools สามารถกิน context window และถูก persist ซ้ำใน conversation ได้; เพิ่ม `compact_tool_result_for_history()` ใน agent loop ด้วย budget 1,600 tokens/8,000 chars และ tests ยืนยันว่า model call ถัดไปเห็น compacted summary แทน raw payload | Implemented |
| 2026-05-13 | Mobile visual audit ต้องใช้ emulated viewport metrics ไม่ใช่เชื่อ headless screenshot ดิบ | Chrome headless `--window-size=375` รายงาน CSS viewport 500px และทำให้ screenshot หลอกว่าหน้าถูกตัด; ใช้ CDP `Emulation.setDeviceMetricsOverride` ตรวจ `innerWidth=375`, `scrollWidth=375` และแก้ mobile width containment ใน CSS | Implemented |
| 2026-05-13 | Context/token visibility ต้องอยู่บน chat header ไม่ใช่ซ่อนใน Memory Panel เท่านั้น | Claude-class product ต้องให้ผู้ใช้เห็นทันทีว่า agent กำลังใช้/trim/compact context อย่างไร; เพิ่ม first-class Context drawer จากปุ่ม header พร้อม ledger card เดียวกับ Memory Panel | Implemented |
| 2026-05-13 | Visual QA ต้องเป็น npm gate ที่รันซ้ำได้ | Manual screenshot ทำให้ regression หลุดง่าย; เพิ่ม `npm run visual:smoke` ที่ start server, seed privacy-safe ledger fixture, ใช้ Chrome CDP desktop/mobile, assert no horizontal overflow, assert Context Inspector render, fail on console/runtime errors | Implemented |
| 2026-05-13 | Tool-result compaction ต้องเป็น tool-aware ไม่ใช่ generic preview | Search/fetch/read/bash/grep/glob มีข้อมูลสำคัญคนละแบบ; เพิ่ม structured history summaries สำหรับ tool เหล่านี้ เพื่อ preserve titles/URLs/file paths/exit codes/previews โดยตัด base64 และ raw payload ใหญ่ | Implemented |
| 2026-05-13 | Memory tests ต้องใช้ runtime path overrides แทน patch internal globals | Test ที่ patch `brain_engine.DB_PATH/get_auto_mem_path` ผูกกับ implementation detail และทำให้ refactor ต่อไปเปราะ; เพิ่ม env overrides `CLAWD_BRAIN_DB_PATH`, `CLAWD_MEMORY_EVENTS_DIR`, `CLAWD_AUTO_MEM_PATH` แล้วปรับ tests ให้ใช้ fixture env | Implemented |
| 2026-05-13 | Skill generation ต้องเป็น review-first control plane ไม่ใช่ generate แล้วใช้ทันที | Hermes-style skill system ที่ production ได้ต้องมี lifecycle/provenance/safety gate; เพิ่ม `.skill-control-plane.json`, statuses `review/active/disabled/archived`, safety scan, usage metadata, และ activate API ก่อน skill เข้า slash command/harness | Implemented |
| 2026-05-13 | Web-generated skills ต้องถูก harness เห็นผ่าน managed skill source เดียวกัน | ก่อนนี้เว็บใช้ `~/.second-brain/skills` แต่ harness loader ไม่อ่าน default นี้ ทำให้ skill ที่สร้างจากเว็บไม่แนบกับ Agent Harness จริง; เพิ่ม managed default + env override `SECOND_BRAIN_SKILLS_DIR` และ filter เฉพาะ active/passed skill | Implemented |
| 2026-05-13 | Visual smoke ต้องตรวจ Skill Control Plane ด้วย ไม่ใช่แค่ chat/context | Session 4 เปลี่ยน surface สำคัญ แต่ smoke เดิมไม่เข้า Skill tab และปล่อย mobile title overlap ได้; เพิ่ม seeded review skill, desktop/mobile skill screenshots, control-plane assertions และแก้ mobile header padding | Implemented |
| 2026-05-13 | Context Inspector ไม่ควรเป็น top-bar control สำหรับผู้ใช้ทั่วไป | Bond ชี้ถูกว่า Context เป็น diagnostic surface; คนใช้ควรเรียกผ่าน `/context` หรือ Memory/Inspector ที่ตั้งใจเปิด ไม่ใช่ icon ค้างบน chat header; ถอดปุ่ม header และเพิ่ม `/context` ใน slash catalog + visual smoke assertion ว่าปุ่มหายจริง | Implemented |
| 2026-05-13 | Generated skill ต้องผ่าน rehearsal/eval ก่อน activate | Safety scan บอกว่า prompt ไม่อันตราย แต่ไม่บอกว่า prompt ใช้ดี; generated/feedback-adjusted skills จึงต้องมี at least one passed evaluation case ก่อนเข้า slash command/harness | Implemented |
| 2026-05-13 | Skill prompt ต้อง rollback และเรียนรู้จาก feedback ได้ | ถ้า skill แก้แล้วแย่ลง ไม่มี revision history = production trap; เพิ่ม revision snapshots, rollback API/UI, usage feedback, และ feedback-driven review draft ที่พา skill กลับเข้า review แทนแก้ active prompt เงียบ ๆ | Implemented |
| 2026-05-13 | Agent Harness tool calls ต้องมี timeout ต่อ tool ไม่ใช่ปล่อยให้ทั้ง turn แขวน | Production web chat รับ request จากผู้ใช้ทั่วไป; tool เดียวที่ค้างต้องถูกแปลงเป็น recoverable tool error ให้ ReAct loop เดินต่อ ไม่ใช่ freeze SSE/ UI | Implemented |
| 2026-05-13 | ReAct/tool telemetry ต้องเป็น first-class web event ไม่ใช่ debug log หลังบ้าน | ถ้า tool call, timeout, loop limit, context pressure มองไม่เห็น ผู้ใช้และ Bond debug ไม่ได้ว่า agent กำลังทำอะไร; เพิ่ม `agent_turn_*`, `tool_timeout`, structured payload, และ Agent trace ใน chat | Implemented |
| 2026-05-13 | Session history ต้อง hydrate tool blocks เป็น Agent trace ไม่ใช่ข้อความ user ปลอม | Conversation persistence เก็บ Anthropic-style `tool_use/tool_result`; frontend เดิม flatten tool result เป็น chat content จนประวัติดูผิดและทำลาย trust | Implemented |
| 2026-05-13 | Web chat ต้องใช้ safe tool profile เป็น default และ registry full-power ต้องอยู่หลัง explicit profile เท่านั้น | Agent Harness มี `Bash/Write/Edit` จริง แต่เว็บสำหรับผู้ใช้ทั่วไปห้าม expose local side-effect tools โดยปริยาย; เพิ่ม `web_safe/research/developer/power_harness`, ให้ web session default `web_safe`, และคง full registry สำหรับ CLI/parity ผ่าน `power_harness` | Implemented |
| 2026-05-13 | Tool permission บนเว็บต้องเป็น explicit approval event ไม่ใช่ headless denial | Permission-required tools ถ้าถูกเรียกผ่าน web ต้องส่ง `tool_permission_request` SSE, รอ approve/deny ผ่าน API, auto-deny เมื่อ timeout, และไม่ leak stack trace; เพิ่ม modal UX + `/api/sessions/{id}/tool-permissions/{request_id}` | Implemented |
| 2026-05-13 | Agent trace ต้อง persist แยกจาก chat transcript และ hydrate ได้หลัง reopen | Tool trace ที่อยู่แค่ runtime หายเมื่อปิดแชท ทำให้ผู้ใช้ย้อนดูไม่ได้ว่า agent ทำอะไร; เพิ่ม bounded JSONL trace ledger per session, API `/agent-traces`, และ frontend merge กับ persisted `tool_use/tool_result` | Implemented |
| 2026-05-13 | Same-session streaming turns ต้องถูกกันที่ backend | UI กันการส่งซ้ำได้ระดับหนึ่งแต่ API ยังยิงซ้อนจน `tool_context/conversation/permission_handler` ปนกันได้; เพิ่ม per-session turn lock และ structured `turn_in_progress` error | Implemented |
| 2026-05-13 | Developer tool profile ห้ามโฆษณา tool ที่ยังไม่ implemented | `RemoteTrigger/NotebookEdit/REPL` และ Windows-only `PowerShell` ทำให้ agent เลือก tool แล้ว fail แบบ dead surface; ถอดออกจาก `developer` profile แต่เก็บใน `power_harness` เพื่อ parity/internal testing | Implemented |
| 2026-05-13 | Launch sidebar ห้ามมี Notebook tab ถ้ายังไม่มี notebook workflow จริง | `Notebook` tab ที่ขึ้น “Coming soon” ทำให้ product ดูเป็น prototype; ถอดออกจาก primary navigation จนกว่า Session 6 จะออกแบบ notebook surface ที่ใช้งานได้จริง | Implemented |
| 2026-05-13 | Session 6: Context Inspector ต้องเรียกผ่าน `/context` ไม่ใช่ปุ่ม header | Context เป็น diagnostic/control-plane surface ไม่ใช่ primary chat action; ปุ่มบนขวาทำให้หน้า chat ดูรกและไม่เหมือน Claude Desktop | Implemented |
| 2026-05-13 | Session 6: Project surface ต้องเป็น goal workspace พร้อม progress rail | Project ที่มีแค่ card/dashboard ไม่พอสำหรับ “AI ทำงานกับเป้าหมาย”; default view ต้องเห็น goals, project chats, memory และ to-do/progress rail แบบ Codex-inspired ทันที | Implemented |
| 2026-05-13 | Session 6: Skill Browser ต้องเป็น control plane ไม่ใช่ grid list | Hermes-style skills ต้องมี filter rail, review/active status, detail panel, activation quality surface และ mobile layout ที่ไม่ตัดข้อความ | Implemented |
| 2026-05-13 | Visual smoke cleanup ต้องไม่ทำให้ QA gate ล้มปลอมหลัง screenshot ผ่าน | Chrome temp profile บน macOS อาจยังเขียนไฟล์หลัง process SIGTERM; script ต้อง wait/kill/retry cleanup แบบ best-effort เพื่อให้ gate สื่อผลจริง | Implemented |
| 2026-05-13 | Session 6 Production Web Surface แตกเป็น 10 remediation sessions ก่อนขยับสู่ production hardening | หลัง Session 6 หลักจบแล้ว ยังมี 100-item UI/product gap ที่ต้องแก้เป็นชุดย่อย Session 1-10: Error/Token Firewall, Streaming Activity, Composer, Scroll, Message Actions, Context Rail, Project Surface, Skills, Navigation/Theme/Open Source, และ QA Gate; เมื่อครบจึงไป Session 7 Security + Local Production Hardening, Session 8 Debloat + File Architecture Cleanup, Session 9 Production Test Matrix + CI Gate, Session 10 Product Polish + Release Candidate | Active |
| 2026-05-13 | Session 6.1 ต้องมี provider error sanitizer และ explicit output token firewall | Live web chat เคยปล่อย OpenRouter 402 raw JSON พร้อม token/credit detail เข้า UI และ agent loop ไม่ส่ง `max_tokens` ทำให้ provider default สูงถึง 65,536 tokens; เพิ่ม `public_error_info()`, agent-loop cap 4,096/8,192, OpenRouter provider cap, และห้าม streaming fallback ยิง provider ซ้ำเมื่อ error จริง | Implemented |
| 2026-05-13 | Session 6.2 Streaming State ต้องแปล harness events เป็น Activity Panel ที่ผู้ใช้เข้าใจ | Raw trace/debug blob ทำให้เว็บดูเป็น dev console ไม่ใช่ product; เพิ่ม `ActivityEvent` contract, map SSE memory/thinking/tool/permission/error/complete เป็น timeline, hydrate persisted tool work กลับมาเป็น activity, และให้ visual smoke assert ว่า activity panel แสดงผลจริง | Implemented |
| 2026-05-13 | Session 6.3 Composer ต้องเป็น compact control ที่ถือ contract mic/send เอง | Claude-class composer ต้องว่างแล้วเห็น mic ไม่เห็น send, พอมี payload ต้องสลับเป็นปุ่มส่งสี primary แบบเนียน และไม่โยน empty submit ไปให้ app/backend กันทีหลัง; ลด height/padding, แยก toolbar left/right, polish attachment chips, constrain slash popup, และเพิ่ม visual smoke assertion สำหรับ mic/send/height | Implemented |
| 2026-05-13 | Session 6.4 Chat Canvas ต้องใช้ pinned-bottom scroll policy ไม่ใช่ force-scroll ทุก token | Force `scrollIntoView` ทุก message change ทำให้ผู้ใช้เลื่อนอ่านแล้วถูกดึงกลับลงล่างและเกิดความรู้สึกเว็บลอย; เพิ่ม scroll viewport ref, pinned-state detection, `overscroll-behavior`, jump-to-latest, และ visual smoke assertion ว่า document ไม่ scroll แทน chat viewport | Implemented |
| 2026-05-13 | Session 6.5 Message actions ต้องทำงานจริงก่อนเพิ่มความสวย | ปุ่ม action ที่เป็นแค่ icon เฉย ๆ ทำให้ product ไม่น่าเชื่อถือ; เพิ่ม copy/edit/retry/fork/remember/feedback row โดย retry ยิง previous user prompt ใหม่, edit โหลด user message เข้า composer, fork สร้าง bounded branch context สำหรับ turn ถัดไป | Implemented |
| 2026-05-13 | Session 6.6 Context + Memory ต้องเป็น rail control surface ไม่ใช่ stats panel | Memory panel เดิมแค่เรียงตัวเลขและ recent memory; redesign เป็น `Context + Memory` rail ที่จัด context budget, review queue, project memory, recent memory เป็น sections พร้อม wider app rail และ smoke assertion ว่า `/context` render rail ใหม่ | Implemented |
| 2026-05-13 | Session 6.7 Project Surface ต้องเป็น command surface พร้อม Codex-style progress ไม่ใช่ dashboard card pile | Claude Project reference แยก title/composer/chats กับ knowledge rail ชัด ส่วน Codex แสดง progress/todo เป็น rail ระหว่างทำงาน; redesign `ProjectView` เป็น Project Memory Active surface, เพิ่ม command rail, next-step card, compact project composer, Agent Harness entry, และอัปเกรด chat progress rail ให้แสดง pinned plan/run status | Implemented |
| 2026-05-13 | Visual QA สำหรับ Project ต้องใช้ isolated DB และมี project fixture จริง | Smoke test เดิมไม่แตะ Project surface และ `ProjectStore` ไม่ honor `CLAWD_BRAIN_DB_PATH` ทำให้ fixture เสี่ยงปนข้อมูลจริง; เพิ่ม env-aware project DB path, seed project/todos/memory ใน visual smoke, และ assert desktop/mobile ว่า command rail/progress/memory capture ไม่ overflow | Implemented |
| 2026-05-13 | Project-scoped memory retrieval ต้องใช้ DB source of truth เดียวกับ writer | `remember()` เคารพ `CLAWD_BRAIN_DB_PATH` แต่ `MemoryManager` เคย query hardcoded `DB_PATH` ทำให้ visual/project rail อ่าน memory คนละฐานกับที่เขียน; แก้ `_conn()` ให้ honor env override และเพิ่ม regression สำหรับ project-scoped memory | Implemented |
| 2026-05-13 | Session 6.8 Navigation/Theme/Open Source polish ต้องทำให้ platform vision โผล่ใน chrome | Bond ต้องการแทน top-left Settings ด้วย Open Source และให้ theme ตามเครื่องแบบ Claude; เพิ่ม Open Source Library entry, theme strip ใน sidebar footer, modal Reusable Intelligence Library พร้อม generate-review-publish-reuse pipeline และ browser/visual smoke guards | Implemented |
| 2026-05-13 | Mobile sidebar ต้องปิดเองหลังเลือก destination | Drawer ที่ค้างทับ project/chat หลังเลือกเมนูทำให้ mobile surface ดู broken; `AppShell` ปิด sidebar เมื่อกด destination button แต่เว้น search row เพื่อไม่ตัด workflow ค้นหา | Implemented |
| 2026-05-13 | Project destructive action ห้ามใช้ browser-default confirm | `window.confirm()` ทำให้ product ดูเป็น prototype และไม่มี copy อธิบาย persistence; แทนด้วย custom delete confirmation modal, ระบุว่า project memories ยังอยู่ใน memory system, และเพิ่ม visual smoke ที่เปิด modal โดยไม่ลบจริง | Implemented |
| 2026-05-13 | Visual smoke matrix ต้องครอบคลุม theme/Open Source/project chat/delete/mobile ไม่ใช่แค่ initial chat | UI regression ของ Session 6 ต้องจับด้วย gate เดียว: desktop dark, desktop light, mobile dark, `/context`, Skill, Open Source modal, Project command rail, Project chat progress rail, custom delete modal, no horizontal overflow, no Context header button | Implemented |
| 2026-05-13 | Session 7 Project Surface ต้องมี failure control plane ไม่ใช่ optimistic UI เงียบ ๆ | Project/todo/memory mutations เดิมถ้า API fail จะหายเงียบหรือให้ frontend เดาสถานะเอง; เพิ่ม inline notice + retry, busy state ต่อ action, delete error ใน modal, todo update API คืน canonical todo และ `jsonFetch` อ่านข้อความ error จาก backend | Implemented |
| 2026-05-13 | Project surface ต้องแสดง execution plan แบบ Codex-style ในหน้า project เอง | Progress rail อย่างเดียวไม่พอเพราะผู้ใช้ต้องเห็น Now/Next/Done ก่อนเปิด chat; เพิ่ม project run preview, workspace sync status, plan row status labels และ visual smoke guard สำหรับ run preview/status labels | Implemented |
| 2026-05-13 | Mobile Project header ต้องมี overlap guard ใน visual smoke | Screenshot เผยว่า hamburger, Back, title และ New Chat ชนกันบน 375px แม้ DOM smoke ผ่าน; แก้ mobile header ให้ซ่อน Back header, จำกัด title width, และเพิ่ม smoke assertion title ไม่ทับ actions/navigation trigger | Implemented |
| 2026-05-13 | Dead controls ใน Project surface ต้องถูกถอด ไม่ใช่ disabled ค้าง | ปุ่ม Project notifications ที่ยังไม่มี workflow ทำให้ UI ดูเป็น prototype; ถอด Bell action ออกจาก Project header จนกว่าจะมี notification system จริง | Implemented |
| 2026-05-13 | Project list/create risk ต้อง preserve data และ retry ได้ ไม่ใช่ทำเหมือนข้อมูลหาย | `refreshProjects()` เดิมล้าง list เมื่อ API fail, New Project modal ไม่มี error/retry, และ sidebar render โปรเจกต์ทั้งหมดเมื่อกด see all; แก้เป็น last-known-good projects + retryable sidebar error, modal error ที่ไม่ทิ้ง draft, create project พร้อม color ใน request เดียว, paginated project list, count pill, active-project visibility guard และ visual smoke large-project assertion | Implemented |
| 2026-05-13 | Session 6.8 Skills Page ต้องเป็น Claude Customize-style control plane ไม่ใช่ list/config page | Skills คือวิธีทำให้ Agent Harness ฉลาดขึ้น ไม่ใช่ settings ย่อย; redesign เป็น Customize topbar, overview cards, status filter rail, searchable skill list, readiness chips, activation switch, lifecycle checklist, trigger/provenance detail, retryable error states และ custom delete confirmation พร้อม visual smoke guard | Implemented |
| 2026-05-13 | Session 6.9 Theme/Open Source ต้องเป็น explicit control plane ไม่ใช่ cycle button/modal เบา ๆ | Theme cycle ทำให้ผู้ใช้เดา state ถัดไปไม่ได้ และ Open Source vision ต้องมี reuse governance/source contract ชัดก่อนเป็น product surface; เพิ่ม System/Light/Dark menu, `data-theme-mode`, Escape-close navigation, readiness metrics, publish contract และ browser/visual guards | Implemented |
| 2026-05-13 | Session 6.10 Production QA ต้องมี command เดียวที่รัน gate ครบ | ถ้า lint/build/backend tests/visual smoke/diff check แยกกัน คนจะลืมรันบางส่วนและ regression หลุด; เพิ่ม `npm run qa:production` ให้เป็น release gate ของ Production Web Surface | Implemented |
| 2026-05-13 | Settings secrets ต้องเป็น replace-only ไม่ใช่ blank-save ได้ | API key field ที่ว่างแล้วกด Save สามารถล้าง provider access และทำให้เว็บตายเหมือนผู้ใช้ทำผิดทั้งที่ UI ชวนให้ผิด; UI ปิด save เมื่อไม่มี key ใหม่, backend ignore blank secret update, และ settings write ใช้ temp file replace | Implemented |
| 2026-05-13 | Production QA ต้องมี static gate + bundle budget + high-volume fixtures | Visual smoke happy path ไม่พอสำหรับ production; เพิ่ม static checker หา `confirm/alert/debugger/dangerouslySetInnerHTML/secret placeholder/literal key`, gzip budget, 37 project fixture, 25 skill fixture, modal semantics และ context ledger containment | Implemented |
| 2026-05-13 | Context Ledger ใน Memory Rail ต้อง constrain Radix ScrollArea wrapper | Screenshot จับได้ว่า ledger cards ล้นออกนอก rail เพราะ ScrollArea child wrapper ขยายตาม max-content; override เฉพาะ `.memory-rail-scroll [data-scroll-viewport] > div` ให้ `display:block;width:100%;min-width:0` และเพิ่ม smoke assertion กัน regression | Implemented |
| 2026-05-13 | Session 7 local web security boundary ต้อง fail closed ที่ Host/client/origin/mutation header | Second Brain web UI เป็น local control plane ไม่ใช่ public API; เพิ่ม loopback Host/client guard, remote bind refusal unless `SECOND_BRAIN_ALLOW_REMOTE_BIND=1`, JSON/body-size gate, `X-Second-Brain-Request` สำหรับ mutation ทุกตัว และ security headers บน JSON/SSE/static | Implemented |
| 2026-05-13 | ถอด `zhipuai` ออกจาก core dependencies จนกว่า SDK จะไม่ pin vulnerable PyJWT range | `pip-audit` พบ `pyjwt 2.8.0` มี `CVE-2026-32597` และ `zhipuai` เวอร์ชันล่าสุดยังบังคับ `pyjwt>=2.8.0,<2.9.0`; เปลี่ยน GLM provider ไปใช้ OpenAI-compatible client และเพิ่ม static gate กัน dependency กลับมา | Implemented |
| 2026-05-13 | Session 8 App shell ต้องเป็น orchestrator ไม่ใช่ dumping ground | `App.tsx` บวมถึง 1,598 บรรทัดและเริ่มถือ modal, stream parsing, chat formatting, theme และ project detection เอง; แยกเป็น components/libs เฉพาะทางจนเหลือ 959 บรรทัด และเพิ่ม line budget gate เพื่อกัน bloat กลับมา | Implemented |
| 2026-05-13 | Session 8 CSS ต้องแยกตาม product surface ไม่ใช่ 7k-line global stylesheet | `index.css` 7,150 บรรทัดทำให้แก้ UI แล้วเสี่ยง cascade regression ทั่วแอป; แยกเป็น 10 surface modules และคง import order เดิมเพื่อให้ review/ownership ชัดขึ้น | Implemented |
| 2026-05-13 | Architecture budget ต้องเป็น production gate ไม่ใช่ความรู้สึกตอน review | ถ้าไม่มี hard gate ไฟล์ใหญ่จะกลับมาบวมใน 1-2 sessions; เพิ่ม line budgets ใน `production-static-check.mjs` สำหรับ App, SkillBrowser, CSS modules, web API/runtime และ session orchestration | Implemented |
| 2026-05-13 | `api.ts` ต้องเป็น facade ไม่ใช่ frontend backend-in-one-file | API client 1,087 บรรทัดทำให้ทุก surface ชนกันและ review ยาก; แยกเป็น domain clients ใต้ `sb-ui/src/lib/api/` แล้วเหลือ facade 8 บรรทัด พร้อม line budgets ต่อ domain | Implemented |
| 2026-05-13 | Chat Canvas ต้องแยก transcript/activity/markdown/progress/empty state | `ChatCanvas.tsx` 969 บรรทัดถือทุกอย่างตั้งแต่ scroll policy ถึง code preview; แยกเป็น 6 chat subcomponents และลดไฟล์หลักเหลือ 248 บรรทัดโดย QA visual smoke ผ่าน | Implemented |
| 2026-05-13 | Project/Skills CSS ต้องแตกอีกชั้นหลัง surface ใหญ่เริ่มนิ่ง | `project.css` และ `skills.css` ยังใหญ่เกินสำหรับ production review แม้แยกจาก index แล้ว; แตกเป็น project sidebar/workspace/command surface และ skills control-plane/modals พร้อม ratchet budget ลง | Implemented |
| 2026-05-14 | Session 9 production gate ต้องเป็น command เดียวที่ CI กับ local ใช้เหมือนกัน | ถ้า lint/build/audit/FastAPI/visual smoke แยกกัน AI หรือคนจะลืมรันบาง gate และ regression จะหลุด; เพิ่ม `npm run qa:production`, GitHub Actions workflow, JSON artifact และ visual screenshot artifacts | Implemented |
| 2026-05-14 | FastAPI web contract tests ห้ามเขียวแบบ skip | ก่อนหน้า local env ไม่มี FastAPI ทำให้ `tests/test_second_brain_web.py` ผ่าน 39 tests แต่ skip TestClient 8 tests; เพิ่ม `scripts/run-fastapi-contracts.py` ที่ติดตั้ง TestClient deps ใน `.qa-cache` และ fail ถ้ายังมี skipped coverage | Implemented |
| 2026-05-14 | Dependency audit ต้องเป็น release gate ไม่ใช่ manual checklist | Security Session 7 เจอ CVE จาก transitive dependency แล้ว; Session 9 จึงเพิ่ม `npm audit --omit=dev --audit-level=high` และ `pip-audit` ผ่าน `scripts/python-dependency-audit.py` เข้า production QA | Implemented |
| 2026-05-14 | Visual smoke ต้องรอ state จริง ไม่ใช้ fixed delay กับ project chat | Fixed delay 500ms ทำให้ mobile-dark project chat flaky; เปลี่ยน smoke ให้ wait จน `.chat-canvas .project-chip`, `.progress-rail`, และ project todo render แล้วค่อย assert | Implemented |
| 2026-05-14 | Production QA ที่มี skipped check ต้อง fail | Release gate ที่อนุญาตให้ env skip audit/visual แล้วเขียวคือ false confidence; `production-qa.mjs` จึง treat skipped checks เป็น failure หลังสรุป report | Implemented |
