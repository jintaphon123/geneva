# Capability Gaps Log

Ongoing log of gaps Claude has identified in its own skill set, context, or tooling within this system.

| Date | Gap Identified | What Triggered It | Proposed Fix | Status |
|------|---------------|-------------------|--------------|--------|
| 2026-04-25 | ไม่มี skill สำหรับ onboarding file ที่วางผิดที่ (เช่น CLAUDE2.md, SKILL.md, reference.md ที่ root) | Session นี้ต้องอ่านและ analyze ไฟล์ 3 ตัวที่วางผิดที่ด้วยตัวเอง ไม่มี SOP รองรับ | สร้าง SOP `references/sops/file-triage.md` — checklist สำหรับ audit ไฟล์แปลกปลอมที่ root แล้ว route ไปที่ถูกต้อง | ✅ Resolved 2026-04-26 |
| 2026-04-29 | `skill-builder` ไม่มี eval loop — ไม่มีทางวัดว่า skill ทำงานได้ดีแค่ไหนก่อน ship | Bond ถาม skill-creator vs skill-builder ต่างกันยังไง → เห็นชัดว่าขาด automated testing, benchmarking, description optimization | Merge `skill-creator` เข้า `skill-builder` — เพิ่ม references/eval-loop.md + references/description-opt.md + scripts + agents ทั้งหมด | ✅ Resolved 2026-04-29 |
| 2026-05-01 | ยังไม่มี shared Gemini sidecar สำหรับ Claude/Codex แม้ Bond จ่าย Google AI Pro อยู่แล้ว | Bond ไม่อยาก copy-paste ข้ามแพลตฟอร์มและต้องการรีด value จาก Gemini Pro ผ่าน MyBrain โดยตรง | สร้าง isolated `codex-gemini-sidecar/` workspace + broker/auto-route/SOP + headless auth wrapper | ✅ Resolved 2026-05-01 |
| 2026-05-13 | Second Brain ไม่มี reusable visual-audit script ที่รัน desktop/mobile/state checks ได้ทันที | ต้องใช้ Browser plugin + Chrome headless/CDP เอง และพบว่า `--window-size=375` หลอกด้วย CSS viewport 500px ถ้าไม่ใช้ device emulation | เพิ่ม `sb-ui` e2e visual smoke script: start server, seed safe fixtures, desktop/mobile screenshots through CDP `Emulation.setDeviceMetricsOverride`, horizontal overflow assertion, console error assertion, attach/settings/mobile-nav/context-inspector checks | ✅ Resolved 2026-05-13 |
| 2026-05-13 | Memory tests patch implementation internals แทนใช้ fixture interface | ระหว่าง split `brain_engine.py` tests พังทันทีเพราะ patch `src.memdir.brain_engine.get_auto_mem_path`; refactor ที่ถูกต้องยังต้อง preserve ชื่อ internal เดิม | สร้าง test fixture/helper กลางสำหรับ temp memory root + db path แล้วให้ production code รับ root provider ผ่าน interface ชัดเจน | ✅ Resolved 2026-05-13 |
| 2026-05-13 | Context Ledger ยังไม่มี UI inspector สำหรับผู้ใช้ดู token/context decisions | Session 3 เพิ่ม backend/API แล้ว แต่ผู้ใช้ยังไม่เห็นว่า memory ไหนถูก inject/trim/compact ระหว่าง turn | สร้าง Context Inspector panel ใน web UI ที่ดึง `/api/context/ledger`, แสดง action/tokens/reason แบบ privacy-safe และผูกกับ memory load event ของ turn ล่าสุด | ✅ Resolved 2026-05-13 |
| 2026-05-13 | Tool-result compaction ยังเป็น generic preview ไม่ใช่ tool-aware summarizer | ระหว่างอุด context bloat ต้องใช้ policy กลางที่ปลอดภัยก่อน แต่ browser/search/file tools ควรมี summary ที่ preserve facts/citations/errors ดีกว่า preview ตัดหัว | เพิ่ม tool-specific history serializers: `WebSearch` เก็บ title/url/snippet, `WebFetch` เก็บ URL/status/headings, file tools เก็บ path/line counts/diff summary, และให้ tests assert no raw 10k-token payload เข้า history | ✅ Resolved 2026-05-13 |
| 2026-05-13 | Visual smoke gate ยังไม่ครอบคลุม feature surface ใหม่ ทำให้ UI regression หลุดได้ | Session 4 เพิ่ม Skill Control Plane แล้ว smoke เดิมยังตรวจแค่ chat/context; screenshot แรกเจอ mobile hamburger ทับหัวข้อ Skills | เพิ่ม smoke path สำหรับ Skill tab: seed review skill ผ่าน API, assert control-plane source/review badge render, capture desktop/mobile skill screenshots, และเพิ่ม mobile safe-left header padding | ✅ Resolved 2026-05-13 |
| 2026-05-13 | Skill Control Plane ยังวัดคุณภาพ skill ไม่ได้ก่อน activate | Review-first + safety scan ยังปล่อย prompt ที่ปลอดภัยแต่ไร้คุณภาพเข้า harness ได้; ก่อน Session 5 ต้องมี evidence ว่า generated skill ผ่าน rehearsal อย่างน้อยหนึ่งเคส | เพิ่ม eval cases, require passed eval ก่อน activate generated/feedback skills, expose quality gate ใน UI, และเพิ่ม API/tests สำหรับ eval contract | ✅ Resolved 2026-05-13 |
| 2026-05-13 | Skill edits ไม่มี rollback/feedback loop | ถ้า generated skill แย่ลงหลังแก้ ไม่มีทางย้อนหรือเก็บ outcome ที่ล้มเหลวกลับมา improve skill; เสี่ยงมากเมื่อ skill เริ่มถูกใช้ใน harness | เพิ่ม revision snapshots, rollback API/UI, feedback records, feedback summary, และ feedback-driven review draft ที่ไม่แก้ active skill เงียบ ๆ | ✅ Resolved 2026-05-13 |
| 2026-05-13 | Web Agent Harness ยังไม่มี stuck-tool safety contract | Session 5 audit เจอว่า tool dispatch เป็น synchronous call ตรง; tool เดียวที่ค้างจะทำให้ SSE turn ค้างและผู้ใช้เห็น product เหมือนตาย | เพิ่ม `ToolSpec.timeout_seconds`, thread-backed timeout wrapper, `tool_timeout` SSE event, ReAct continuation via tool error result, และ regression test สำหรับ slow tool | ✅ Resolved 2026-05-13 |
| 2026-05-13 | Reopened chat history แสดง tool result ผิดเป็น user message | ระหว่างเชื่อม Agent trace พบว่า frontend `flattenContent()` แปลง `tool_result` block เป็นข้อความในแชท ทำให้ session ที่ใช้ tools ดูเหมือน user พูด output เอง | แยก hydrate logic: assistant `tool_use` → `toolEvents`, user `tool_result` → attach กลับเข้า trace, ไม่ render เป็น bubble หลัก; visual smoke seed session tool trace | ✅ Resolved 2026-05-13 |
| 2026-05-13 | Web tool registry ไม่มี safe/default profile boundary | การเอา Agent Harness มาแนบเว็บแบบตรง ๆ เสี่ยง expose `Bash/Write/Edit` ให้ web user โดยไม่ตั้งใจ หรือทำให้ CLI parity พังถ้าปิดหมด | เพิ่ม `web_safe/research/developer/power_harness` profiles; web session default `web_safe`; CLI/default registry ยังเป็น full `power_harness`; API `/api/tools?profile=` มี tests | ✅ Resolved 2026-05-13 |
| 2026-05-13 | Tool permission UX ยังเป็น backend concept ไม่ใช่ product event | ถ้า tool ต้อง approval บนเว็บ ผู้ใช้ไม่มีจุด approve/deny และ headless denial ทำให้เหมือน agent ล้มเหลวเอง | เพิ่ม `tool_permission_request/resolved` SSE, approval modal, resolve API, timeout auto-deny, and unit tests for permission request/resolution | ✅ Resolved 2026-05-13 |
| 2026-05-13 | Agent trace หายหลังปิด/reopen session | Runtime trace แสดงใน turn สด แต่ reopened session ไม่รู้ duration/error/output preview ที่เกิดจริง | เพิ่ม bounded per-session JSONL agent trace store, `/agent-traces` API, frontend trace lookup/merge, and bounded-store tests | ✅ Resolved 2026-05-13 |
| 2026-05-13 | Same-session stream concurrency ยัง corrupt state ได้ | API สามารถยิงสอง turns เข้า session เดียวพร้อมกัน ทำให้ shared `tool_context.permission_handler`, conversation, and trace state race กัน | เพิ่ม per-session turn lock ที่ reject concurrent worker ด้วย structured `turn_in_progress` error และ regression test | ✅ Resolved 2026-05-13 |
| 2026-05-13 | Developer profile ยัง expose dead tools | `RemoteTrigger`, `NotebookEdit`, `REPL`, and Windows-only `PowerShell` เป็น tool ที่ agent เห็นแต่ใช้จริงไม่ได้ใน production path | ถอด dead tools จาก `developer`; เก็บไว้เฉพาะ `power_harness` สำหรับ internal parity/testing; เพิ่ม test กัน regression | ✅ Resolved 2026-05-13 |
| 2026-05-13 | Sidebar ยังมี Notebook dead tab | Scan เจอ `Coming soon` ใน primary navigation ซึ่งขัดกับ launch rule ว่าทุกปุ่มต้องทำงานจริง | ถอด Notebook tab/placeholder ออกจาก launch nav และ visual smoke ยืนยัน layout desktop/mobile ใหม่ | ✅ Resolved 2026-05-13 |
| 2026-05-13 | Visual QA ยังไม่กันปุ่ม Context header กลับมา | Session 6 ต้องถอด top-right Context ตาม Bond แต่ถ้าไม่มี assertion จะ regression กลับมาได้ง่าย | เพิ่ม visual smoke assertion ว่าไม่มี `Toggle context inspector` และไม่มี `Toggle memory panel`; `/context` ต้องเปิด Context Inspector แทน | ✅ Resolved 2026-05-13 |
| 2026-05-13 | Project/Skill surfaces ยังเป็น app-shell ธรรมดา ไม่ใช่ production workspace | Claude/Codex reference ชี้ว่าหน้า Project ต้องมี progress rail และ Skill ต้องเป็น control plane; layout เดิมยังให้กลิ่น side project | Redesign ProjectView เป็น goal workspace + right progress rail, ChatCanvas เป็น project progress rail/normal small tab, SkillBrowser เป็น filter/list/detail control plane | ✅ Resolved 2026-05-13 |
| 2026-05-13 | Visual smoke cleanup สามารถ fail หลังตรวจผ่านจริง | Chrome profile cleanup เจอ `ENOTEMPTY` แล้ว process exit 1 ทั้งที่ screenshots/assertions ผ่าน | เพิ่ม graceful process termination, retrying `safeRm()`, และ cleanup warning ที่ไม่ทำให้ successful smoke กลายเป็น false failure | ✅ Resolved 2026-05-13 |
| 2026-05-13 | Browser Use typing path ยังไม่ reliable สำหรับ local UI ถ้า virtual clipboard ไม่พร้อม | Session 6.3 เปิด local UI ผ่าน in-app Browser ได้ แต่ `locator.fill()`/typing composer สะดุดที่ Browser Use virtual clipboard/CDP timeout ทั้งที่ Chrome CDP visual smoke ตรวจ state เดียวกันผ่าน | ใช้ `npm run visual:smoke` เป็น authoritative UI gate ระหว่างนี้ และเพิ่ม Browser Use fallback helper ที่ใช้ DOM input event/CDP โดยไม่พึ่ง clipboard ก่อนใช้เป็น manual QA หลัก | Open |
